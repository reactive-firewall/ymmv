#! /bin/bash --posix

# version 20140517

umask 137

GREP_ARGS=""

while [[ ($# -gt 1) && (${1:0:1} = '-') ]] ; do
GREP_ARGS="${GREP_ARGS} ${1}"
shift
done

\grep${GREP_ARGS} -oE "([[:alnum:]]{1}[-[:alnum:]]*[\.]{1}){2,}[[:alnum:]]{1}[-[:alnum:]\.]+" $@

exit 0 ;